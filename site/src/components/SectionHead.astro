---
interface Props {
  index: string;
  kicker: string;
  heading: string;
  id?: string;
}

const { index, kicker, heading, id } = Astro.props;
---
<p class="kicker">{index} &middot; {kicker}</p>
<h2 id={id}>{heading}</h2>
