---
interface Props {
  file: string;
  active?: boolean;
}

const { file, active = true } = Astro.props;
---
{active ? (
  <div class="stub">
    <span class="stub-tag">Placeholder &middot; {file}</span>
    <slot />
  </div>
) : (
  <slot />
)}
